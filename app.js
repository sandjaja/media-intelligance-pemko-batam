const API_BASE = window.MEDIA_INTELLIGENCE_API || '/api';
const state = { tab:'dashboard', opd:'all', overpower:false, metrics:null, articles:[], highlights:[], alerts:[], opdList:[], health:null, sources:[] };
const tabs=[['dashboard','gauge-high','Dashboard Utama'],['highlights','fire','Daily News Highlights'],['printarchive','newspaper','Media Cetak'],['ownedchannels','tower-broadcast','Owned Channel'],['sowhat','brain','So What? Engine'],['sources','database','Manajemen Data & Isu'],['ask','comments','Ask Intelligence']];
const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>\'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));async function api(path,options={}){const r=await fetch(API_BASE+path,{credentials:'include',...options});if(!r.ok)throw new Error((await r.json().catch(()=>({}))).error||`HTTP ${r.status}`);return r.json()}function toast(msg){const t=$('toast');if(!t)return;t.textContent=msg;t.classList.remove('hidden');clearTimeout(window.__toast);window.__toast=setTimeout(()=>t.classList.add('hidden'),3500)}function renderTabs(){$('tabs').innerHTML=tabs.map(t=>`<button data-tab="${t[0]}" class="tab px-3 py-2 rounded-lg border border-transparent text-xs text-slate-400 hover:text-white"><i class="fa-solid fa-${t[1]} mr-2"></i>${t[2]}</button>`).join('')+(state.overpower?'<button data-tab="sowhat" class="tab px-3 py-2 rounded-lg border border-amber-500/40 text-amber-300 text-xs"><i class="fa-solid fa-microchip mr-2"></i>Deep Intelligence</button>':'');document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>showTab(b.dataset.tab));document.querySelector(`[data-tab="${state.tab==='curation'?'ownedchannels':state.tab==='scan'?'printarchive':state.tab}"]`)?.classList.add('active')}function showTab(id){state.tab=id;document.querySelectorAll('main section').forEach(s=>s.classList.add('hidden'));$(id)?.classList.remove('hidden');renderTabs();if(id==='dashboard')drawChart();if(id==='printarchive')window.renderPrintArchive?.();if(id==='scan')window.renderPrintClippingWorkspace?.();if(id==='curation')window.renderOwnedPublicationCuration?.();if(id==='ownedchannels')window.renderOwnedChannelIntelligence?.();window.dispatchEvent(new CustomEvent('media-intelligence-tab',{detail:id}))}function riskBadge(level='low'){const map={critical:'bg-rose-500/20 text-rose-300 border-rose-500/40',high:'bg-orange-500/20 text-orange-300 border-orange-500/40',medium:'bg-amber-500/20 text-amber-300 border-amber-500/40',low:'bg-slate-500/20 text-slate-300 border-slate-600'};return `<span class="px-2 py-1 rounded border text-[10px] font-black uppercase ${map[level]||map.low}">${esc(level)}</span>`}function card(title,value,sub,icon){return `<div class="glass rounded-xl p-4"><div class="flex justify-between"><div><p class="text-xs text-slate-400">${title}</p><h3 class="text-2xl font-black mt-1">${value}</h3></div><i class="fa-solid fa-${icon} text-cyan-400 text-lg"></i></div><p class="text-[11px] text-slate-400 mt-3">${sub}</p></div>`}function renderDashboard(){const m=state.metrics||{},news=state.highlights.length?state.highlights:state.articles.slice(0,5);$('dashboard').innerHTML=`<div class="glass rounded-2xl p-5"><span class="text-[10px] text-cyan-400 font-black tracking-widest">EXECUTIVE BRIEF</span><h2 class="text-xl font-bold mt-1">Media Intelligence Executive Summary</h2><p class="text-xs text-slate-400 mt-1">${state.opd==='all'?'Seluruh Pemko Batam':esc(state.opd)}</p></div><div class="grid grid-cols-2 lg:grid-cols-4 gap-4">${card('Total Berita Diproses',m.total_articles??0,'Database/API','newspaper')}${card('Sumber Media',m.sources??0,'Outlet terdaftar','globe')}${card('Media Momentum',`${m.momentum??0}/100`,'Average importance','gauge-high')}${card('Critical Issues',m.critical_alerts??0,'Perlu atensi segera','triangle-exclamation')}</div>`}function drawChart(){}function renderHighlights(){const news=state.highlights.length?state.highlights:state.articles;$('highlights').innerHTML=news.length?news.map((n,i)=>`<article class="glass rounded-2xl p-5"><div class="text-xs text-slate-400">RANK #${i+1} · ${esc(n.source_name||'Unknown')}</div><h3 class="font-bold text-lg mt-3">${esc(n.title)}</h3></article>`).join(''):'<div class="glass rounded-2xl p-6 text-sm text-slate-400">Belum ada berita.</div>'}function renderScan(){}function renderSoWhat(){$('sowhat').innerHTML='<div class="glass rounded-2xl p-5"><h2 class="text-xl font-bold">So What? Intelligence Engine</h2></div>'}function renderSources(){$('sources').innerHTML='<div class="glass rounded-2xl p-5"><h2 class="text-xl font-bold">Manajemen Data & Isu</h2><p class="text-xs text-slate-400 mt-2">Master sumber media, keyword OPD, klasifikasi dan isu.</p></div>'}function renderAsk(){$('ask').innerHTML='<div class="glass rounded-2xl p-5"><h2 class="text-xl font-bold">Ask Intelligence</h2></div>'}async function load(){try{const opd=await api('/opd');state.opdList=opd.data||[];$('opdSelect').innerHTML='<option value="all">Semua OPD / Pemko Batam</option>'+state.opdList.map(o=>`<option value="${esc(o.id)}">${esc(o.name)}</option>`).join('');const qs=state.opd==='all'?'':'?opdId='+encodeURIComponent(state.opd);const[d,a,h,alerts,health]=await Promise.all([api('/dashboard'+qs),api('/articles'+qs),api('/highlights'+qs),api('/alerts'+qs),api('/ingestion/status')]);state.metrics=d.metrics||{};state.articles=a.data||[];state.highlights=h.data||[];state.alerts=alerts.data||[];state.health=health;state.sources=health.sources||[];renderDashboard();renderHighlights();renderSources();renderAsk();$('alertText').textContent=`API aktif · ${state.articles.length} artikel · ${state.highlights.length} highlight · ${state.alerts.length} open alert.`}catch(e){$('alertText').textContent='API belum terhubung atau autentikasi belum tersedia.';toast(e.message);renderDashboard();renderHighlights();renderSources();renderAsk()}}$('opdSelect').onchange=async e=>{state.opd=e.target.value;await load()};$('modeBtn').onclick=()=>{state.overpower=!state.overpower;renderTabs()};$('exportBtn').onclick=()=>toast('Gunakan menu laporan untuk export.');renderTabs();setInterval(()=>$('clock').textContent=new Date().toLocaleString('id-ID'),1000);$('clock').textContent=new Date().toLocaleString('id-ID');load();