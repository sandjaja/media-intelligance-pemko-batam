(()=>{'use strict';const API=(window.MEDIA_INTELLIGENCE_API||'/api').replace(/\/$/,''),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(path,opts={}){const r=await fetch(API+path,{credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json'},...opts}),j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.detail||j.error||`HTTP ${r.status}`);return j}function root(){return document.getElementById('sources')}function shell(sub){return window.dataIssueShell?.shell?.('issues',sub)||''}function bind(el){window.dataIssueShell?.bindShell?.(el)}const badge=(label,value)=>`<span class="inline-flex items-center gap-1 px-2 py-1 rounded-full bg-slate-900 border border-slate-800 text-[10px] text-slate-300"><b class="text-slate-500">${label}</b> ${esc(value??'-')}</span>`;
async function openIssues(){const el=root();if(!el)return;el.classList.remove('hidden');el.innerHTML=shell('issues')+'<div class="glass rounded-2xl p-5">Memuat manajemen isu...</div>';bind(el);try{const [ir,tr]=await Promise.all([api('/admin/issue-taxonomy/issues'),api('/admin/issue-taxonomy/taxonomies')]),tax=tr.data||[],issues=ir.data||[];el.innerHTML=shell('issues')+`<div class="glass rounded-2xl p-5 mb-4"><div class="text-[10px] tracking-widest text-violet-400 font-black">MANAJEMEN ISU</div><h2 class="text-lg font-black mt-1">Daftar Isu & Klasifikasinya</h2></div><div class="space-y-4">${issues.map(i=>`<article class="glass rounded-2xl p-5" data-row="${i.id}"><div class="flex flex-wrap justify-between gap-3"><div><div class="text-[10px] tracking-widest text-violet-400 font-black">ISSUE #${i.id}</div><h3 class="font-black text-base mt-1">${esc(i.title)}</h3><div class="mt-2 text-xs text-violet-300">Taxonomy: ${esc(i.taxonomy_name||'Belum diklasifikasikan')}</div></div><div>${badge('Status',i.status)}</div></div><details class="mt-4 rounded-xl border border-slate-800"><summary class="cursor-pointer px-4 py-3 text-xs font-bold text-cyan-300">Edit detail isu</summary><div class="p-4 border-t border-slate-800 space-y-3"><input data-title value="${esc(i.title)}" class="w-full bg-slate-950 border border-slate-700 rounded-lg px-3 py-2"><textarea data-description rows="3" class="w-full bg-slate-950 border border-slate-700 rounded-lg px-3 py-2">${esc(i.description||'')}</textarea><select data-tax class="w-full bg-slate-950 border border-slate-700 rounded-lg px-3 py-2"><option value="">Belum diklasifikasikan</option>${tax.filter(t=>t.active||Number(t.id)===Number(i.taxonomy_category_id)).map(t=>`<option value="${t.id}" ${Number(i.taxonomy_category_id)===Number(t.id)?'selected':''}>${esc(t.name)}</option>`).join('')}</select><button data-save class="px-4 py-2 bg-violet-500 rounded-lg text-xs font-bold">SIMPAN</button></div></details></article>`).join('')}</div>`;bind(el);el.querySelectorAll('[data-row]').forEach(row=>row.querySelector('[data-save]').onclick=async()=>{await api(`/admin/issue-taxonomy/issues/${row.dataset.row}`,{method:'PATCH',body:JSON.stringify({title:row.querySelector('[data-title]').value.trim(),description:row.querySelector('[data-description]').value.trim()||null,taxonomyCategoryId:row.querySelector('[data-tax]').value?Number(row.querySelector('[data-tax]').value):null})});window.toast?.('Isu diperbarui.');openIssues()})}catch(e){el.innerHTML=shell('issues')+`<div class="glass rounded-2xl p-5 text-rose-300">${esc(e.message)}</div>`;bind(el)}}
function routeText(k){const r=k.routing||[],p=r.find(x=>x.routing_role==='PRIMARY'),s=r.filter(x=>x.routing_role==='SUPPORTING');return p?`Utama: ${esc(p.opd_name)}${s.length?' · Pendukung: '+s.map(x=>esc(x.opd_name)).join(', '):''}`:'belum routing OPD'}
async function openTax(){const el=root();if(!el)return;el.classList.remove('hidden');el.innerHTML=shell('classification')+'<div class="glass rounded-2xl p-5">Memuat Master Klasifikasi...</div>';bind(el);try{const d=(await api('/admin/issue-taxonomy/master')).data||{},sectors=d.sectors||[],tax=d.taxonomies||[],keywords=d.keywords||[],opds=d.opds||[],routeKeywords=keywords.filter(k=>(k.routing||[]).length).length;el.innerHTML=shell('classification')+`<div class="glass rounded-2xl p-5 mb-4"><div class="text-[10px] tracking-widest text-violet-400 font-black">MASTER KLASIFIKASI</div><h2 class="text-lg font-black mt-1">Sektor → Taxonomy → Master Keyword → Routing OPD</h2><div class="grid grid-cols-2 md:grid-cols-4 gap-2 mt-4">${[['SEKTOR',sectors.length],['TAXONOMY',tax.length],['MASTER KEYWORD',new Set(keywords.map(k=>String(k.id))).size],['KEYWORD PUNYA ROUTING',routeKeywords]].map(x=>`<div class="rounded-xl bg-slate-950/60 border border-slate-800 p-3"><b class="text-xl">${x[1]}</b><div class="text-[10px] text-slate-500">${x[0]}</div></div>`).join('')}</div></div><div class="space-y-3">${sectors.map((s,si)=>{const st=tax.filter(t=>Number(t.sector_id)===Number(s.id));return `<details class="glass rounded-2xl overflow-hidden" ${si===0?'open':''}><summary class="cursor-pointer p-4 flex justify-between gap-3"><div><div class="text-[10px] text-violet-400 font-black">SEKTOR ${String(si+1).padStart(2,'0')} ${s.active?'':'· NONAKTIF'}</div><div class="font-black mt-1">${esc(s.name)}</div></div><span class="text-xs text-slate-500">${st.length} taxonomy</span></summary><div class="border-t border-slate-800 p-3 space-y-2">${st.map(t=>{const kw=keywords.filter(k=>Number(k.category_id)===Number(t.id));return `<details class="rounded-xl border border-slate-800 bg-slate-950/40"><summary class="cursor-pointer p-3"><div class="flex justify-between gap-2"><div><div class="text-[10px] text-slate-500">${esc(t.code)} ${t.active?'':'· NONAKTIF'}</div><b class="text-sm">${esc(t.name)}</b></div>${badge('Keyword',kw.length)}</div></summary><div class="border-t border-slate-800 p-3 space-y-2">${kw.map(k=>`<div class="rounded-lg border ${Number(k.weight)>=8?'border-emerald-500/30 bg-emerald-500/10':Number(k.weight)>=5?'border-cyan-500/30 bg-cyan-500/10':'border-slate-700 bg-slate-900'} px-3 py-2" data-keyword="${k.id}"><div class="flex items-start justify-between gap-2"><div><div class="text-xs font-bold">${esc(k.keyword)} ${k.active?'':'· NONAKTIF'}</div><div class="text-[9px] text-slate-500">Bobot ${esc(k.weight)} · ${routeText(k)}</div></div><button data-route-open class="shrink-0 px-2 py-1 rounded border border-violet-500/40 text-[10px] text-violet-300">ATUR ROUTING</button></div><div data-route-editor class="hidden mt-3 border-t border-slate-700 pt-3 space-y-2"><label class="block text-[10px] text-slate-400">Jenis OPD<select data-group class="mt-1 w-full bg-slate-950 border border-slate-700 rounded-lg px-2 py-2"><option value="DINAS">Dinas</option><option value="BADAN">Badan</option><option value="SETDA">Setda</option><option value="LAINNYA">Lainnya</option></select></label><label class="block text-[10px] text-slate-400">OPD Utama<select data-primary class="mt-1 w-full bg-slate-950 border border-slate-700 rounded-lg px-2 py-2"></select></label><div class="text-[10px] text-slate-400">OPD Pendukung (maks. 3)</div><div data-support class="grid grid-cols-1 gap-1"></div><button data-route-save class="px-3 py-2 bg-violet-500 rounded-lg text-[10px] font-bold">SIMPAN ROUTING</button></div></div>`).join('')||'<span class="text-xs text-slate-500">Belum ada master keyword.</span>'}</div></details>`}).join('')}</div></details>`}).join('')}</div>`;bind(el);el.querySelectorAll('[data-keyword]').forEach(row=>{const k=keywords.find(x=>String(x.id)===row.dataset.keyword),editor=row.querySelector('[data-route-editor]'),group=row.querySelector('[data-group]'),primary=row.querySelector('[data-primary]'),support=row.querySelector('[data-support]');function draw(){const list=opds.filter(o=>o.active&&o.opd_group===group.value),routing=k.routing||[],p=routing.find(x=>x.routing_role==='PRIMARY'),ss=new Set(routing.filter(x=>x.routing_role==='SUPPORTING').map(x=>String(x.opd_id)));primary.innerHTML='<option value="">Belum ditentukan</option>'+list.map(o=>`<option value="${o.id}" ${p&&String(p.opd_id)===String(o.id)?'selected':''}>${esc(o.name)}</option>`).join('');support.innerHTML=list.map(o=>`<label class="flex items-center gap-2 text-[10px] p-1"><input type="checkbox" data-support-id="${o.id}" ${ss.has(String(o.id))?'checked':''}> ${esc(o.name)}</label>`).join('')||'<span class="text-[10px] text-slate-500">Tidak ada OPD aktif pada jenis ini.</span>'}row.querySelector('[data-route-open]').onclick=()=>{editor.classList.toggle('hidden');const p=(k.routing||[]).find(x=>x.routing_role==='PRIMARY');if(p)group.value=p.opd_group||'LAINNYA';draw()};group.onchange=draw;row.querySelector('[data-route-save]').onclick=async()=>{const ids=[...support.querySelectorAll('[data-support-id]:checked')].map(x=>Number(x.dataset.supportId));if(ids.length>3)return window.toast?.('Maksimal 3 OPD pendukung.');await api(`/admin/issue-taxonomy/master/keywords/${k.id}/routing`,{method:'PUT',body:JSON.stringify({primaryOpdId:primary.value?Number(primary.value):null,supportingOpdIds:ids})});window.toast?.('Routing OPD disimpan.');openTax()}})}catch(e){el.innerHTML=shell('classification')+`<div class="glass rounded-2xl p-5 text-rose-300">${esc(e.message)}</div>`;bind(el)}}window.openIssueManagement=openIssues;window.openTaxonomyManagement=openTax})();