import type { FastifyInstance, FastifyRequest } from 'fastify';
import type { Pool } from 'pg';
import jwt from 'jsonwebtoken';
import { z } from 'zod';
import { loadAuthorizationContext, type AuthorizationContext } from './rbac.js';

declare module 'fastify' { interface FastifyRequest { communicationGapAuth?: AuthorizationContext } }

type ExternalSignal={id:string;title:string|null;summary:string|null;body_text:string|null;sentiment:string|null;risk_score:string|number;importance_score:string|number;edition_date:string;source_name:string;opd_id:string|null;opd_name:string|null};
type OwnedCluster={id:string;canonical_title:string|null;representative_title:string|null;representative_content:string|null;member_count:number|string;channel_count:number|string;first_published_at:string|null;last_published_at:string|null};

const STOP=new Set(['dan','yang','di','ke','dari','untuk','pada','dengan','atau','ini','itu','dalam','atas','sebagai','oleh','kota','batam','pemko','pemerintah','berita','halaman']);
const ISSUE_TERMS=[['apbd','rapbd','rancangan apbd','nota keuangan','tahun anggaran'],['pad','pendapatan asli daerah','pendapatan daerah','pajak daerah'],['banjir','genangan','tergenang','drainase'],['jalan','ruas jalan','lalu lintas','kemacetan','macet'],['sampah','limbah','kebersihan'],['investasi','investor','pertumbuhan ekonomi','ekonomi'],['pelayanan publik','layanan publik','mal pelayanan publik'],['kemiskinan','pengentasan kemiskinan'],['pendidikan','sekolah','siswa','mahasiswa'],['kesehatan','rumah sakit','puskesmas'],['olahraga','popda','porda','porprov','atlet','sport tourism','kepemudaan'],['judi online','judol','perjudian online']];
const ANGLE_TERMS=[['defisit','kekurangan','selisih'],['surplus'],['target','capaian','realisasi'],['kenaikan','naik','meningkat'],['penurunan','turun','menurun'],['kritik','keluhan','aduan','dikeluhkan'],['gangguan','terhambat','terputus'],['penanganan','ditangani','perbaikan','diperbaiki'],['prestasi','penghargaan'],['risiko','ancaman','darurat']];
function norm(v:string|null|undefined){return String(v??'').toLowerCase().replace(/https?:\/\/\S+/g,' ').replace(/[^a-z0-9\s]/g,' ').replace(/\s+/g,' ').trim()}
function padded(v:string|null|undefined){return ` ${norm(v)} `}
function hasPhrase(text:string,term:string){return text.includes(` ${norm(term)} `)}
function toks(v:string|null|undefined,max=500){return norm(v).split(' ').filter(x=>x.length>2&&!STOP.has(x)).slice(0,max)}
function jaccard(a:string[],b:string[]){const A=new Set(a),B=new Set(b);if(!A.size||!B.size)return 0;let n=0;for(const x of A)if(B.has(x))n++;return n/(A.size+B.size-n)}
function containment(a:string[],b:string[]){const A=new Set(a),B=new Set(b);if(!A.size||!B.size)return 0;let n=0;for(const x of A)if(B.has(x))n++;return n/Math.min(A.size,B.size)}
function groups(text:string|null|undefined,taxonomy:string[][]){const p=padded(text);return taxonomy.map((terms,i)=>terms.some(t=>hasPhrase(p,t))?i:-1).filter(i=>i>=0)}
function overlap(a:number[],b:number[]){if(!a.length||!b.length)return 0;const B=new Set(b),n=a.filter(x=>B.has(x)).length;return n/Math.min(a.length,b.length)}
function similarity(signal:ExternalSignal,cluster:OwnedCluster){const st=toks(signal.title,80),sb=toks(`${signal.summary||''} ${signal.body_text||''}`,500),ct=toks(cluster.canonical_title||cluster.representative_title,80),cb=toks(cluster.representative_content,500);const title=jaccard(st,ct),body=jaccard(sb,cb),contain=Math.max(containment(st,ct),containment(sb,cb));return Math.max(title,.88*body,.82*contain,.35*title+.65*body)}
function issueAware(signal:ExternalSignal,cluster:OwnedCluster){const lexical=similarity(signal,cluster);const sCore=groups(`${signal.title||''} ${signal.summary||''}`,ISSUE_TERMS);const cCore=groups(`${cluster.canonical_title||''} ${cluster.representative_title||''}`,ISSUE_TERMS);const sSupport=groups(signal.body_text,ISSUE_TERMS);const cSupport=groups(cluster.representative_content,ISSUE_TERMS);const coreIssueScore=overlap(sCore,cCore);const supportIssueScore=Math.max(overlap(sCore,cSupport),overlap(sSupport,cCore));const sameIssue=coreIssueScore>0;const sAngles=groups(`${signal.title||''} ${signal.summary||''}`,ANGLE_TERMS);const cAngles=groups(`${cluster.canonical_title||''} ${cluster.representative_title||''}`,ANGLE_TERMS);const angleScore=overlap(sAngles,cAngles);const score=sameIssue?Math.max(lexical,.68+.18*coreIssueScore+.08*angleScore):lexical;const matchType=!sameIssue?'lexical':sAngles.length===0?'same_issue_angle_unknown':angleScore>0?'same_issue_same_angle':'same_issue_different_angle';return{score:Math.min(1,score),lexical,coreIssueScore,supportIssueScore,angleScore,matchType,sameIssue}}
function priority(s:ExternalSignal,status:string){const risk=Number(s.risk_score||0),importance=Number(s.importance_score||0),negative=String(s.sentiment||'').toLowerCase()==='negative';const raw=risk*.55+importance*.25+(negative?20:0)+(status==='gap'?15:status==='partial'?7:0);return raw>=70?'critical':raw>=52?'high':raw>=34?'medium':'watch'}
function operationalRecommendation(s:ExternalSignal,status:string,p:string,response:OwnedCluster|null,angleCoverage='none'){const leadOpd=s.opd_name||'Humas/Media Center bersama OPD teknis terkait';const negative=String(s.sentiment||'').toLowerCase()==='negative';const risk=Number(s.risk_score||0),importance=Number(s.importance_score||0);const sla=p==='critical'?'≤ 1 jam':p==='high'?'≤ 3 jam':p==='medium'?'≤ 6 jam':'≤ 24 jam';if(status==='gap')return{leadOpd,urgency:p,targetResponseTime:sla,reason:`Belum ditemukan narasi resmi yang cukup relevan${negative?', sementara sentimen eksternal negatif':''}. Risk ${risk}/100 dan importance ${importance}/100.`,objective:'Mengisi kekosongan informasi resmi dengan fakta terverifikasi sebelum narasi eksternal berkembang tanpa konteks Pemko.',actions:['Validasi fakta dan kondisi terbaru dengan OPD teknis/petugas lapangan.',p==='critical'||p==='high'?'Siapkan holding statement singkat segera; jangan menunggu rilis panjang selesai.':'Siapkan pernyataan resmi ringkas berbasis fakta dan langkah penanganan.','Publikasikan respons pada kanal utama Pemko/Media Center, lalu tautkan ke OPD terkait.','Pantau perubahan sentimen dan pemberitaan setelah respons dipublikasikan.'],channelStrategy:'Pemko/Media Center sebagai sumber utama; OPD teknis sebagai penguat fakta dan tindak lanjut.'};if(angleCoverage==='different')return{leadOpd,urgency:p,targetResponseTime:sla,reason:`Isu pokok sudah direspons resmi, tetapi angle media eksternal belum dijawab secara spesifik. Risk ${risk}/100 dan importance ${importance}/100.`,objective:'Menutup angle gap tanpa membuat narasi baru yang bertentangan dengan pesan resmi yang sudah ada.',actions:['Pertahankan narasi resmi yang sudah ada sebagai sumber utama.','Verifikasi fakta khusus pada angle media eksternal dan siapkan data penjelas.','Tambahkan klarifikasi/FAQ atau update singkat yang secara eksplisit menjawab angle tersebut.','Amplifikasi klarifikasi melalui kanal Pemko dan OPD relevan bila diperlukan.'],channelStrategy:'Tidak perlu mengulang seluruh rilis; gunakan update/clarification yang menutup angle media secara spesifik.'};if(status==='partial')return{leadOpd,urgency:p,targetResponseTime:sla,reason:`Narasi resmi sudah ada tetapi baru terdistribusi di ${Number(response?.channel_count||1)} kanal.`,objective:'Memperluas jangkauan narasi resmi dan memastikan pesan kunci konsisten lintas kanal.',actions:['Verifikasi bahwa narasi resmi menjawab inti isu eksternal.','Amplifikasi melalui kanal utama Pemko/Media Center dan kanal OPD relevan.','Samakan headline, fakta kunci, data, dan call-to-action antar kanal.','Pantau apakah pemberitaan eksternal mulai mengutip atau merujuk respons resmi.'],channelStrategy:'Perlu amplifikasi lintas kanal resmi; hindari membuat versi pesan yang saling berbeda.'};return{leadOpd,urgency:'monitor',targetResponseTime:'Monitoring berkala',reason:`Narasi resmi sudah muncul di ${Number(response?.channel_count||0)} kanal dengan ${Number(response?.member_count||0)} publikasi.`,objective:'Menjaga konsistensi fakta dan memastikan amplifikasi tidak menghasilkan pesan yang saling bertentangan.',actions:['Pantau konsistensi pesan dan perkembangan sentimen eksternal.','Perbarui narasi hanya jika ada fakta atau perkembangan baru.','Catat kanal yang paling efektif untuk pola amplifikasi berikutnya.'],channelStrategy:'Tidak perlu menambah respons baru bila isu sudah terjawab; fokus pada konsistensi dan update faktual.'}}

export async function registerCommunicationGapRoutes(app:FastifyInstance,pool:Pool,jwtSecret:string){const auth=async(request:FastifyRequest,reply:any)=>{const token=request.cookies.access_token;if(!token)return reply.code(401).send({error:'UNAUTHENTICATED'});try{const decoded=jwt.verify(token,jwtSecret) as jwt.JwtPayload;if(typeof decoded.sub!=='string')throw new Error('invalid');const ctx=await loadAuthorizationContext(pool,decoded.sub);if(!ctx?.active)return reply.code(403).send({error:'ACCOUNT_INACTIVE'});request.communicationGapAuth=ctx}catch{return reply.code(401).send({error:'INVALID_ACCESS_TOKEN'})}};app.get('/api/intelligence/communication-gaps',{preHandler:auth},async(request,reply)=>{const parsed=z.object({days:z.coerce.number().int().min(1).max(30).default(7),limit:z.coerce.number().int().min(1).max(100).default(50),threshold:z.coerce.number().min(.2).max(.8).default(.35)}).safeParse(request.query);if(!parsed.success)return reply.code(400).send({error:'INVALID_QUERY'});const {days,limit,threshold}=parsed.data;
const external=(await pool.query<ExternalSignal>(`SELECT pa.id,pa.title,pa.summary,pa.body_text,pa.sentiment,pa.risk_score,pa.importance_score,pe.edition_date::text,ms.name source_name,pa.opd_id,o.name opd_name FROM print_articles pa JOIN print_editions pe ON pe.id=pa.edition_id JOIN media_sources ms ON ms.id=pe.source_id LEFT JOIN opd o ON o.id=pa.opd_id WHERE pa.status IN ('verified','analyzed') AND pe.edition_date >= CURRENT_DATE-$1::int ORDER BY pa.risk_score DESC,pa.importance_score DESC,pe.edition_date DESC LIMIT $2`,[days,limit])).rows;
const clusters=(await pool.query<OwnedCluster>(`SELECT occ.id,occ.canonical_title,occ.member_count,occ.channel_count,occ.first_published_at,occ.last_published_at,sm.title representative_title,sm.content representative_content FROM owned_content_clusters occ LEFT JOIN social_mentions sm ON sm.id=occ.representative_mention_id WHERE COALESCE(occ.last_published_at,occ.first_published_at,now()) >= now()-($1::text||' days')::interval ORDER BY occ.last_published_at DESC NULLS LAST`,[String(days+3)])).rows;
const gaps=external.map(s=>{let best:OwnedCluster|null=null,bestMatch:any=null;for(const c of clusters){const m=issueAware(s,c);if(!bestMatch||m.score>bestMatch.score){best=c;bestMatch=m}}const matched=best&&bestMatch&&(bestMatch.sameIssue||bestMatch.score>=Math.max(threshold,.55));const matchedCluster=matched?best:null;const channels=matchedCluster?Number(matchedCluster.channel_count||0):0;const status=!matchedCluster?'gap':channels>1?'amplified':'partial';const angleCoverage=!matchedCluster?'none':bestMatch.matchType==='same_issue_different_angle'?'different':bestMatch.matchType==='same_issue_same_angle'?'same':'unknown';const p=priority(s,status);return{external:{id:s.id,title:s.title,sourceName:s.source_name,editionDate:s.edition_date,sentiment:s.sentiment,riskScore:Number(s.risk_score||0),importanceScore:Number(s.importance_score||0),opdId:s.opd_id,opdName:s.opd_name},response:matchedCluster?{clusterId:matchedCluster.id,title:matchedCluster.canonical_title||matchedCluster.representative_title,channelCount:channels,publicationCount:Number(matchedCluster.member_count||0),similarity:Number(bestMatch.score.toFixed(4)),lexicalSimilarity:Number(bestMatch.lexical.toFixed(4)),issueSimilarity:Number(bestMatch.coreIssueScore.toFixed(4)),supportIssueSimilarity:Number(bestMatch.supportIssueScore.toFixed(4)),angleSimilarity:Number(bestMatch.angleScore.toFixed(4)),matchType:bestMatch.matchType,angleCoverage,firstPublishedAt:matchedCluster.first_published_at,lastPublishedAt:matchedCluster.last_published_at}:null,status,priority:p,recommendation:operationalRecommendation(s,status,p,matchedCluster,angleCoverage)}}).sort((a,b)=>{const p:any={critical:4,high:3,medium:2,watch:1};return p[b.priority]-p[a.priority]||b.external.riskScore-a.external.riskScore});
const pickupItems=clusters.map(c=>{const matches=external.map(s=>({s,m:issueAware(s,c)})).filter(x=>x.m.sameIssue||x.m.score>=Math.max(threshold,.55)).sort((a,b)=>b.m.score-a.m.score);const best=matches[0]||null;const sources=[...new Set(matches.map(x=>x.s.source_name).filter(Boolean))];return{official:{clusterId:c.id,title:c.canonical_title||c.representative_title,publicationCount:Number(c.member_count||0),channelCount:Number(c.channel_count||0),firstPublishedAt:c.first_published_at,lastPublishedAt:c.last_published_at},pickup:best?{articleId:best.s.id,title:best.s.title,sourceName:best.s.source_name,editionDate:best.s.edition_date,similarity:Number(best.m.score.toFixed(4)),matchType:best.m.matchType,mediaSourceCount:sources.length,mediaSources:sources}:null,status:best?'picked_up':'pickup_gap',recommendation:best?{action:'monitor',message:`Narasi resmi sudah terangkat media eksternal melalui ${sources.length} sumber media terpantau.`}:{action:'distribute',message:'Narasi resmi belum ditemukan pada media eksternal terpantau. Pertimbangkan distribusi rilis, media pitch, atau follow-up ke media yang relevan.'}}}).sort((a,b)=>a.status===b.status?String(b.official.lastPublishedAt||'').localeCompare(String(a.official.lastPublishedAt||'')):a.status==='pickup_gap'?-1:1);
const summary={externalSignals:gaps.length,gaps:gaps.filter(x=>x.status==='gap').length,partial:gaps.filter(x=>x.status==='partial').length,amplified:gaps.filter(x=>x.status==='amplified').length,angleGaps:gaps.filter(x=>x.response?.angleCoverage==='different').length,highPriority:gaps.filter(x=>x.priority==='high'||x.priority==='critical').length,actionRequired:gaps.filter(x=>x.status==='gap'||x.response?.angleCoverage==='different').filter(x=>x.priority==='critical'||x.priority==='high'||x.priority==='medium').length,windowDays:days,threshold};
const pickupSummary={officialNarratives:pickupItems.length,pickupGaps:pickupItems.filter(x=>x.status==='pickup_gap').length,pickedUp:pickupItems.filter(x=>x.status==='picked_up').length,pickupRate:pickupItems.length?Number(((pickupItems.filter(x=>x.status==='picked_up').length/pickupItems.length)*100).toFixed(1)):0,externalUniverse:'print_media_only',windowDays:days};
return{data:{summary,items:gaps,pickupSummary,pickupItems,engine:'communication-gap-rule-v1.4-bidirectional-coverage'}}})}
