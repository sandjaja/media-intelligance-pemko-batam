(()=>{'use strict';
const API=(window.MEDIA_INTELLIGENCE_API||'/api').replace(/\/$/,'');
let lastId=null,lastServerState='',checking=false,lastCheck=0;
function clippingId(){const marker=document.querySelector('#paEdit [class*="text-cyan"]');const m=(marker?.textContent||'').match(/CLIPPING\s*#(\d+)/i);return m?Number(m[1]):null;}
function stateOf(a){const x=a?.ai_metadata?.v16Routing;return `${a?.status||''}:${x?.routingStatus||''}:${x?.keywordVerification||''}`;}
function pendingUi(a){const x=a?.ai_metadata?.v16Routing;if(a?.status!=='verified'||x?.routingStatus!=='ROUTED'||x?.keywordVerification!=='PENDING')return false;const btn=document.getElementById('paProcessAnalysis');if(btn)btn.remove();const box=document.getElementById('printReviewWorkflow');if(box){const title=box.querySelector('.text-sm.font-bold');if(title)title.textContent='Routing V16.5 selesai. Verifikasi keyword untuk menyimpan hasil analisis.';let note=document.getElementById('printKeywordPendingWorkflow');if(!note){note=document.createElement('div');note.id='printKeywordPendingWorkflow';note.className='mt-4 py-3 rounded-xl border border-amber-500/30 bg-amber-500/5 text-center text-xs font-black text-amber-300';note.textContent='MENUNGGU TERIMA KEYWORD / EDIT KEYWORD';box.appendChild(note);}}
return true;}
function forceWorkflowRefresh(){document.getElementById('printKeywordPendingWorkflow')?.remove();document.getElementById('printReviewWorkflow')?.remove();}
async function sync(force=false){const id=clippingId();if(!id){lastId=null;lastServerState='';return;}const now=Date.now();if(checking||(!force&&now-lastCheck<500))return;checking=true;lastCheck=now;try{const r=await fetch(`${API}/print/articles/${id}?_state=${now}`,{credentials:'include',cache:'no-store'});if(!r.ok)return;const j=await r.json(),a=j.data||{},s=stateOf(a),changed=id!==lastId||s!==lastServerState;lastId=id;lastServerState=s;if(pendingUi(a))return;if(changed&&a.status==='analyzed'){forceWorkflowRefresh();}else if(changed&&a.status==='verified'&&!a.ai_metadata?.v16Routing){forceWorkflowRefresh();}}catch{}finally{checking=false;}}
new MutationObserver(()=>sync()).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',e=>{if(e.target.closest?.('#paProcessAnalysis,#printKeywordAccept,#printKeywordSave,#paReopenCorrection,[data-view]')){setTimeout(()=>sync(true),150);setTimeout(()=>sync(true),700);setTimeout(()=>sync(true),1600);}},true);setInterval(()=>sync(),1000);sync(true);
})();