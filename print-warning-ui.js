(()=>{
const API=(window.MEDIA_INTELLIGENCE_API||'/api').replace(/\/$/,'');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function clippingId(){const marker=document.querySelector('#paEdit [class*="text-cyan"]');const m=(marker?.textContent||'').match(/CLIPPING\s*#(\d+)/i);return m?Number(m[1]):null;}
function isV16RoutingOnly(){const anchor=document.getElementById('printAnalysisResult');return Boolean(anchor&&/HASIL ROUTING\s*·\s*V16\.5/i.test(anchor.textContent||''));}
function fieldCard(label){return [...document.querySelectorAll('#printAnalysisResult .rounded-lg.bg-slate-950')].find(el=>String(el.textContent||'').trim().startsWith(label));}
function humanizeEvidenceSource(anchor){const rows=[...anchor.querySelectorAll('div')];const row=rows.find(el=>/^\s*Evidence Source:/i.test(el.textContent||''));const b=row?.querySelector('b');if(!b)return;const raw=String(b.textContent||'').trim();const labels={TITLE_SUMMARY:'Judul + Ringkasan',TITLE_OCR_LEAD:'Judul + Cuplikan OCR'};b.textContent=labels[raw]||raw.replaceAll('_',' ');const label=row.querySelector('span');if(label)label.textContent='Sumber Analisis:';}
async function jsonFetch(url,opt={}){const r=await fetch(url,{credentials:'include',cache:'no-store',headers:{...(opt.body?{'Content-Type':'application/json'}:{})},...opt});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.message||j.error||`HTTP ${r.status}`);return j;}
function removeKeywordReview(){document.getElementById('printKeywordReview')?.remove();}
async function finalizeKeyword(id,keywordId,button){if(button?.disabled)return;const box=document.getElementById('printKeywordReview');try{box?.querySelectorAll('button,select').forEach(el=>el.disabled=true);if(button)button.innerHTML='<i class="fa-solid fa-spinner fa-spin mr-1"></i>Menyimpan...';const body=keywordId?{keywordId:Number(keywordId)}:{};const j=await jsonFetch(`${API}/print/articles/${id}/finalize-keyword`,{method:'POST',body:JSON.stringify(body)});const x=j.analysis||j.data?.ai_metadata?.v16Routing||{};if(box){box.className='mt-3 rounded-xl border border-emerald-500/30 bg-emerald-500/5 p-4';box.innerHTML=`<div class="text-[10px] font-black tracking-widest text-emerald-300">KEYWORD TERVERIFIKASI · TERKUNCI</div><div class="mt-2 text-sm font-bold">${esc(x.keyword||'Keyword tersimpan')}</div><div class="mt-1 text-[10px] text-slate-400">${x.keywordSource==='MANUAL'?'Keyword diedit dan routing dihitung ulang dari Master Classification.':'Keyword hasil V16.5 diterima.'} Data analisis telah disimpan sebagai final.</div>`;}window.toast?.('Keyword tersimpan. Clipping telah dikunci sebagai Analyzed.');setTimeout(()=>window.dispatchEvent(new CustomEvent('media-intelligence-tab',{detail:'printmedia'})),100);}catch(e){box?.querySelectorAll('button,select').forEach(el=>el.disabled=false);if(button)button.textContent=keywordId?'Simpan Keyword':'Terima Keyword';window.toast?.('Penyimpanan keyword gagal: '+e.message);}}
async function editKeyword(id,x){const box=document.getElementById('printKeywordReview');if(!box)return;try{box.innerHTML='<div class="text-xs text-cyan-300"><i class="fa-solid fa-spinner fa-spin mr-1"></i>Memuat Master Keyword...</div>';const j=await jsonFetch(`${API}/print/keyword-options`);const options=(j.data||[]);if(!options.length)throw new Error('Master Keyword aktif tidak ditemukan.');const unique=new Map();options.forEach(k=>{const id=String(k.id);if(!unique.has(id))unique.set(id,k)});box.innerHTML=`<div class="text-[10px] font-black tracking-widest text-amber-300">EDIT KEYWORD · MASTER CLASSIFICATION</div><p class="mt-1 text-[10px] text-slate-400">Pilih Master Keyword pengganti. Taxonomy, Primary OPD, Supporting OPD dan UPTD akan dihitung ulang.</p><select id="printKeywordSelect" class="mt-3 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-xs">${[...unique.values()].map(k=>`<option value="${esc(k.id)}" ${String(k.id)===String(x.keywordId)?'selected':''}>${esc(k.keyword)} · ${esc(k.taxonomy_name||'-')} · ${esc(k.opd_name||'-')}</option>`).join('')}</select><div class="mt-3 flex flex-wrap gap-2"><button id="printKeywordSave" class="px-3 py-2 rounded-lg bg-amber-500 text-slate-950 text-xs font-black">Simpan Keyword</button><button id="printKeywordCancel" class="px-3 py-2 rounded-lg border border-slate-700 text-xs">Batal</button></div>`;document.getElementById('printKeywordSave').onclick=e=>finalizeKeyword(id,document.getElementById('printKeywordSelect').value,e.currentTarget);document.getElementById('printKeywordCancel').onclick=()=>renderKeywordReview(id,x);}catch(e){box.innerHTML=`<div class="text-xs text-rose-300">Gagal memuat Master Keyword: ${esc(e.message)}</div><button id="printKeywordCancel" class="mt-2 px-3 py-2 rounded-lg border border-slate-700 text-xs">Kembali</button>`;document.getElementById('printKeywordCancel').onclick=()=>renderKeywordReview(id,x);}}
function renderKeywordReview(id,x){removeKeywordReview();if(!x||x.routingStatus!=='ROUTED'||x.keywordVerification!=='PENDING')return;const anchor=document.getElementById('printAnalysisResult');if(!anchor)return;const box=document.createElement('div');box.id='printKeywordReview';box.className='mt-3 rounded-xl border border-amber-500/30 bg-amber-500/5 p-4';box.innerHTML=`<div class="flex flex-wrap justify-between gap-2"><div><div class="text-[10px] font-black tracking-widest text-amber-300">VERIFIKASI KEYWORD</div><div class="mt-1 text-sm font-bold">${esc(x.keyword||'-')}</div><div class="mt-1 text-[10px] text-slate-400">Periksa keyword hasil V16.5 sebelum hasil analisis disimpan dan dikunci.</div></div><span class="text-[10px] px-2 py-1 rounded border border-amber-500/30 self-start">Menunggu Konfirmasi</span></div><div class="mt-3 flex flex-wrap gap-2"><button id="printKeywordAccept" class="px-3 py-2 rounded-lg bg-emerald-600 text-xs font-black"><i class="fa-solid fa-check mr-1"></i>Terima Keyword</button><button id="printKeywordEdit" class="px-3 py-2 rounded-lg bg-amber-500 text-slate-950 text-xs font-black"><i class="fa-solid fa-pen mr-1"></i>Edit Keyword</button></div>`;anchor.after(box);document.getElementById('printKeywordAccept').onclick=e=>finalizeKeyword(id,null,e.currentTarget);document.getElementById('printKeywordEdit').onclick=()=>editKeyword(id,x);}
async function enrichV16Names(){const id=clippingId(),anchor=document.getElementById('printAnalysisResult');if(!id||!anchor||!isV16RoutingOnly())return;humanizeEvidenceSource(anchor);try{const r=await fetch(`${API}/print/articles/${id}`,{credentials:'include',cache:'no-store'});if(!r.ok)return;const j=await r.json(),a=j.data||{},x=a.ai_metadata?.v16Routing;if(!x)return;
 const primary=fieldCard('Primary OPD')?.querySelector('b');if(primary&&(x.primaryOpdName||a.opd_name))primary.textContent=x.primaryOpdName||a.opd_name;
 const supporting=fieldCard('Supporting OPD')?.querySelector('b');if(supporting&&Array.isArray(x.supportingOpds)&&x.supportingOpds.length)supporting.textContent=x.supportingOpds.map(o=>o?.name||o?.code).filter(Boolean).join(', ');
 let uptd=document.getElementById('printV16Uptd');if(!uptd){uptd=document.createElement('div');uptd.id='printV16Uptd';uptd.className='mt-2 rounded-lg bg-slate-950 p-3';const evidence=[...anchor.querySelectorAll('div')].find(el=>/Evidence Source:/i.test(el.textContent||'')||/Sumber Analisis:/i.test(el.textContent||''));if(evidence?.parentElement)evidence.parentElement.before(uptd);else anchor.appendChild(uptd);}const units=Array.isArray(x.uptdMatches)?x.uptdMatches:[];uptd.innerHTML=`<div class="text-[10px] text-slate-500">UPTD Terdeteksi</div><b class="text-xs">${units.length?units.map(u=>esc(u?.name||u?.code||'-')).join(', '):'-'}</b>`;
 anchor.dataset.namedRouting='1';renderKeywordReview(id,x);}catch{}}
function render(s){document.getElementById('phase2fWarningSignal')?.remove();const anchor=document.getElementById('printAnalysisResult');if(!anchor||!s||isV16RoutingOnly())return;const candidate=Boolean(s.candidateAlert);const box=document.createElement('div');box.id='phase2fWarningSignal';box.className=`mt-3 rounded-xl border ${candidate?'border-amber-500/30 bg-amber-500/5':'border-emerald-500/20 bg-emerald-500/5'} p-4`;box.innerHTML=`<div class="flex flex-wrap justify-between gap-2"><div><div class="text-[10px] font-black tracking-widest ${candidate?'text-amber-300':'text-emerald-300'}">EARLY WARNING · PHASE 2F</div><div class="mt-1 text-sm font-bold">${esc(s.warningLevel)} · ${Math.round(Number(s.warningScore||0))}/100</div></div><span class="text-[10px] px-2 py-1 rounded border border-slate-700 self-start">Evidence ${esc(s.evidenceConfidence||'LOW')}</span></div><div class="grid grid-cols-3 gap-2 mt-3 text-center"><div class="rounded-lg bg-slate-950 p-3"><div class="text-[10px] text-slate-500">Candidate Alert</div><b class="text-xs ${candidate?'text-amber-300':'text-emerald-300'}">${candidate?'YA':'TIDAK'}</b></div><div class="rounded-lg bg-slate-950 p-3"><div class="text-[10px] text-slate-500">Evidence</div><b class="text-xs">${Number(s.evidenceSourceCount||0)}</b></div><div class="rounded-lg bg-slate-950 p-3"><div class="text-[10px] text-slate-500">Issue Evidence</div><b class="text-xs">${s.issueId?'#'+s.issueId:'-'}</b></div></div><div class="mt-3 text-[11px] text-slate-400"><b class="text-slate-300">Alasan:</b> ${(s.reasons||[]).map(esc).join(' · ')||'-'}</div><div class="mt-2 text-[10px] text-slate-500">${esc(s.note||'')}</div><div class="mt-2 text-[9px] text-slate-600">${esc(s.engine||'phase2f-warning-signal')}</div>`;anchor.after(box);}
let refreshing=false,lastKey='';async function refresh(){if(refreshing)return;const id=clippingId();const anchor=document.getElementById('printAnalysisResult');if(!id||!anchor)return;if(isV16RoutingOnly()){document.getElementById('phase2fWarningSignal')?.remove();const key=`${id}:${anchor.textContent?.length||0}`;if(key!==lastKey||!anchor.dataset.namedRouting){refreshing=true;try{await enrichV16Names();lastKey=key;}finally{refreshing=false;}}return;}removeKeywordReview();const existing=document.getElementById('phase2fWarningSignal');if(existing?.dataset.articleId===String(id))return;try{const r=await fetch(`${API}/print/articles/${id}/warning-signal`,{credentials:'include',cache:'no-store'});if(!r.ok)return;const j=await r.json();render(j.data);const box=document.getElementById('phase2fWarningSignal');if(box)box.dataset.articleId=String(id);}catch{}}
new MutationObserver(()=>refresh()).observe(document.body,{childList:true,subtree:true});setInterval(refresh,1200);refresh();
})();
(()=>{if(document.querySelector('script[data-print-document-review-v3]'))return;const s=document.createElement('script');s.src='./print-document-review-v3.js?v=20260917-docfirst1';s.dataset.printDocumentReviewV3='1';document.head.appendChild(s);})();
